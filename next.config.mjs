/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    //if icon image is not working then use this
    images: {
        unoptimized: true
    }
};

export default nextConfig;
